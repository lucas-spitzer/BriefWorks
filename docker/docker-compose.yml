services:
  redis:
    image: redis:7-alpine
    container_name: briefworks-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - briefworks_redis_data:/data
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 5s
      timeout: 3s
      retries: 5

volumes:
  briefworks_redis_data:
